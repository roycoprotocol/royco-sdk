import { ContractAddresses } from "./contract-addresses";
import { ContractAbis } from "./contract-abis";

export const Contracts_80094 = {
  DepositExecutor: {
    address: ContractAddresses.DepositExecutor,
    abi: ContractAbis.DepositExecutor,
  },
} as const;
